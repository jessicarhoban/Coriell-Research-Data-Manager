# June 27, 2024

# create env
mamba create -n research_app

# Add bioconda channel
conda config --env --add channels bioconda

# (Get package list from other environment, put in working directory)
# conda list --export > packagelist.txt

# Install packages from other environment
conda install --file packagelist.txt
